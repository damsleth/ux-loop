export const stylePreset = `
Style preset: enterprise
- Favor dense but readable layouts, explicit hierarchy, and conservative interaction patterns.
- Prioritize WCAG AA contrast, predictable spacing, and clear affordances.
- Avoid novelty that could reduce clarity or trust.
`.trim()
