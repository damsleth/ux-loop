{
  "name": "@damsleth/ux-loop",
  "version": "1.2.0",
  "private": false,
  "type": "module",
  "description": "Drop-in agentic UX pipeline (shots -> review -> implement) for npm projects.",
  "license": "WTFPL",
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "files": [
    "src",
    "README.md",
    "CHANGELOG.md"
  ],
  "bin": {
    "uxl": "src/cli.mjs"
  },
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test",
    "release:check": "npm test && npm pack --dry-run"
  },
  "peerDependencies": {
    "axe-core": ">=4.0.0",
    "playwright": ">=1.59.0"
  },
  "peerDependenciesMeta": {
    "axe-core": {
      "optional": true
    }
  }
}
